<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.example.p5rconfidantdialoguev2">

    <application
        android:name=".Database.ConfidantApplication"
        android:allowBackup="true"
        android:icon="@mipmap/p5d"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/p5d"
        android:supportsRtl="true"
        android:theme="@style/Theme.P5RConfidantDialogueV2">
        <activity android:name=".TextFinderActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>